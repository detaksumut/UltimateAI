/**
 * ProgressReporter is a helper class to report execution progress of workflows or steps.
 */
export class ProgressReporter {
  report(progress: number): void {
    // Console log or emit progress update event.
  }
}
